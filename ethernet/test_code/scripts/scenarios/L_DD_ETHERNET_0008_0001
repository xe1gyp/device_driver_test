# Begin L_DD_ETHERNET_0008_0001

01  set -x; handlerIfconfig.sh "get" "ipaddr"
02  set -x; handlerNfs.sh "verify"

# Process Parallelism N Commands No Delay

03  set -x; handlerProcessParallelism.sh "clean"

04  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
05  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
06  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
07  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
08  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
09  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
10  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
11  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
12  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
13  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"

14  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.01 bs=1M count=5"
15  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.02 bs=1M count=5"
16  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.03 bs=1M count=5"
17  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.04 bs=1M count=5"
18  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.05 bs=1M count=5"
19  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.06 bs=1M count=5"
20  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.07 bs=1M count=5"
21  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.08 bs=1M count=5"
22  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.09 bs=1M count=5"
23  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.10 bs=1M count=5"

24  set -x; handlerProcessParallelism.sh "execute"

# Process Parallelism N Commands Random Delay

25  set -x; handlerProcessParallelism.sh "clean"

26  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
27  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
28  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
29  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
30  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
31  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
32  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
33  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
34  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"
35  set -x; handlerProcessParallelism.sh "add" "ping -c 25 -s $ETHERNET_PACKET_SIZE `cat $ETHERNET_EXTERNAL_HOST_IPADDR`"

36  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.01 bs=1M count=5"
37  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.02 bs=1M count=5"
38  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.03 bs=1M count=5"
39  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.04 bs=1M count=5"
40  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.05 bs=1M count=5"
41  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.06 bs=1M count=5"
42  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.07 bs=1M count=5"
43  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.08 bs=1M count=5"
44  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.09 bs=1M count=5"
45  set -x; handlerProcessParallelism.sh "add" "dd if=$ETHERNET_DEV_URANDOM of=`cat $ETHERNET_NFS_DIR`/eth.10 bs=1M count=5"

46  set -x; handlerProcessParallelism.sh "execute" "withrandomdelay"

# End L_DD_ETHERNET_0008_0001

