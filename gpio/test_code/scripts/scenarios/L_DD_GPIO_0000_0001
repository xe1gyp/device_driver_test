##############################################################################
# Scenario    : L_DD_GPIO_0000_0001
# Author      : Cristopherson Torres | Leed Aguilar
# Date        : 02 Frebuary 2010
# Description : Device Controller: GPIO driver shall support all the GPIO
#               hardware modules available
##############################################################################

# Begin L_DD_GPIO_0000_0001

0001 set -x; $UTILS_DIR_SCRIPTS/system_message_buffer.sh "system.log" && exit 0 || exit 1
0002 set -x; cat "system.log" | grep -i "gpio" && exit 0 || exit 1
0003 set -x; rm "system.log" && exit 0 || exit 1

# End L_DD_GPIO_0000_0001
