###############################################################################
# Scenario: L_DD_RTC_0004
# Author  : Misael Lopez Cruz
# Date    : Octuber 5, 2006
###############################################################################

# Begin L_DD_RTC_0004

0001	   set -x; echo -e "5\n" | $APP_PER_INT $DEVFS_RTC | grep  '1 2 3 4 5' && exit 0 || exit 1
0002_01  set -x; echo -e "$APP_VAL_INPUT_USER\n $APP_VALID_DATE\n $APP_VALID_TIME\n" | $APP_TIME_SET $DEVFS_RTC && $APP_TIME_GET $DEVFS_RTC | grep "${MSG_OK_DATE_TIME_DEFAULT} ${RTC_VALID_DATE}" && exit 0 || exit 1
0002_02	 set -x; echo -e "$ALARM_SEC\n $ALARM_VALUE\n" | $APP_ALARM_SET $DEVFS_RTC && exit 0 || exit 1

# End L_DD_RTC_0004