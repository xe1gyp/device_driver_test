################################################################################
# Scenario: L_DD_USBOTG_0001_2_core
# Author  : Axel Castaneda Gonzalez
# Date    : January  2, 2006
# Description: Test OMAP as a usb-host. (Gadget Zero)
#	Connecting and disconnecting the cable, detecting if the usb host
#	is detected and enumerated correctly.
# ChangeLog:
#	9 Jan 2009 - Diego Zavala Trujillo - Changing the scenario structure.
#		enum.sh has been added to detect specifics errors.
################################################################################

foo	echo 'L_DD_USBOTG_0001_2_core'

1  echo -e '\n\n\t Please connect cable. [ Mini-A to OMAP - Mini-B to an USB Device (Mass Storage) ] \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh connectDevice &&  exit 0 || exit 1

2  echo -e '\n\n\t Disconnect cable. \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectDevice  &&  exit 0 || exit 1

3  echo -e '\n\n\t Please connect cable  again. \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh connectDevice &&  exit 0 || exit 1

4  echo -e '\n\n\t Disconnect cable. \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectDevice  &&  exit 0 || exit 1

bar	echo 'L_DD_USBOTG_0001_2_core'
