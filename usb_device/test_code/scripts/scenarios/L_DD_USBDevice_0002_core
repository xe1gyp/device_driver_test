################################################################################
# Scenario: L_DD_USBDevice_0002_core
# Author  : Axel Castaneda Gonzalez
# Date    : December 4, 2006
# Description: Testing enumeration with a standard host core base.
# ChangLog:
#	27 Jan 2009 - Diego Zavala Trujillo - Update Scenario.
#		Change the struture add enum.sh script to call funtions.
################################################################################

foo	echo L_DD_USBDevice_0002_core

# For L_DD_USBDevice_0001 export ENUM_INFO="Zero"
# For L_DD_USBDevice_0002 export ENUM_INFO="Gadget Serial"
# For L_DD_USBDevice_0004 export ENUM_INFO="Ethernet Gadget"
# For L_DD_USBDevice_0005 export ENUM_INFO="File-backed Storage Gadget"

1  echo -e '\n\n\t Please connect cable. [ MiniB to OMAP - StdA to Linux PC ] \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh connectHost  && exit 0 || exit 1

2  echo -e '\n\n\t Checking enumeration on the Linux PC \n\n' && $TESTSCRIPT/enum.sh connectHostRemote  &&  exit 0 || exit 1

3  echo -e '\n\n\t Disconnect USB cable from OMAP \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectHostRemote  &&  exit 0 || exit

4  echo -e '\n\n\t Please connect cable. [ MiniB to OMAP - StdA to Linux PC ] \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh connectHost  && exit 0 || exit 1

5  echo -e '\n\n\t Checking enumeration on the Linux PC \n\n' && $TESTSCRIPT/enum.sh connectHostRemote  &&  exit 0 || exit 1

6  echo -e '\n\n\t Disconnect USB cable from OMAP \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectHostRemote  &&  exit 0 || exit

#7  set -x; echo -e '\n\n\t Please connect cable. [ MiniB to OMAP - StdA to Linux PC ] \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh connectHost  && exit 0 || exit 1

#8 echo -e '\n\n\t Checking enumeration on the Linux PC \n\n' && $TESTSCRIPT/enum.sh connectHostRemote  &&  exit 0 || exit 1

#9 echo -e '\n\n\t Disconnect USB cable from OMAP \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectHostRemote  &&  exit 0 || exit

#10 set -x; echo -e '\n\n\t Please connect cable. [ MiniB to OMAP - StdA to Linux PC ] \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh connectHost  && exit 0 || exit 1

#11 echo -e '\n\n\t Checking enumeration on the Linux PC \n\n' && $TESTSCRIPT/enum.sh connectHostRemote  &&  exit 0 || exit 1

##12 echo -e '\n\n\t Disconnect USB cable from OMAP \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectHost  &&  exit 0 || exit

#13 echo -e '\n\n\t Disconnect USB cable from OMAP \n\n'  &&  sleep $DELAY3  &&  $TESTSCRIPT/enum.sh disconnectHostRemote  &&  exit 0 || exit

bar	echo L_DD_USBDevice_0002_core
