###################################################################################################################
# Scenario: L_DD_WDT_0002A
# Author  : Misael Lopez Cruz
# Date    : March 10, 2007
# Description: Tests the various ioctls supported by watchdog timer driver.
#              Kernel image must be compiled with "Disable watchdog shutdown on close".
###################################################################################################################

# Begin L_DD_WDT_0002A

0001	($TESTBIN/$TESTWDTIOCTL &) && echo "Board is going to be rebooted!" && sleep 3 && killall $TESTWDTIOCTL && exit 0 || exit 1

# End L_DD_WDT_0002A
