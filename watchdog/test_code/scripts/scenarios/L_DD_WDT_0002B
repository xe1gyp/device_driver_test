###################################################################################################################
# Scenario: L_DD_WDT_0002B
# Author  : Misael Lopez Cruz
# Date    : March 10, 2007
# Description: Tests the various ioctls supported by watchdog timer driver.
#              Kernel image must be compiled with "Disable watchdog shutdown on close".
###################################################################################################################

# Begin L_DD_WDT_0002B

0001	echo "VALIDATION CRITERIA: Board should be reset after executing L_DD_WDT_0002A scenario." && sleep 1 && $UTILBIN/akey && exit 0 || exit 1

# end L_DD_WDT_0002B
