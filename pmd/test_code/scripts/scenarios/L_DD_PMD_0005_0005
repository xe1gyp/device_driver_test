###############################################################################
# Scenario: L_DD_POWER_0005_0005
# Testing : OPPs Control: Locking/Confirming OPPs
###############################################################################

# Begin L_DD_POWER_0005_0005

01  set -x; smartReflexAutoComp.sh  enable  VDD1
02  set -x; smartReflexAutoComp.sh  enable  VDD2

03  set -x; oppControl.sh  lock  VDD1  1
04  set -x; oppControl.sh  lock  VDD1  2
05  set -x; oppControl.sh  lock  VDD1  3
06  set -x; oppControl.sh  lock  VDD1  4
07  set -x; oppControl.sh  lock  VDD1  5
08  set -x; oppControl.sh  lock  VDD1  0

09  set -x; oppControl.sh  lock  VDD2  1  && exit 1 || exit 0
10  set -x; oppControl.sh  lock  VDD2  2  && exit 0 || exit 1
11  set -x; oppControl.sh  lock  VDD2  3  && exit 0 || exit 1
12  set -x; oppControl.sh  lock  VDD2  4  && exit 1 || exit 0
13  set -x; oppControl.sh  lock  VDD2  5  && exit 1 || exit 0
14  set -x; oppControl.sh  lock  VDD2  0  && exit 1 || exit 0

15  set -x; smartReflexAutoComp.sh  disable  VDD1
16  set -x; smartReflexAutoComp.sh  disable  VDD2

# End L_DD_POWER_0005_0005
