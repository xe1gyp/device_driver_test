###################################################################################################################
# Scenario: L_DD_NAND_0003
# Author  : Misael Lopez Cruz
# Date    : August 29th, 2006
# Description: Erase operation tests
###################################################################################################################

# Begin L_DD_NAND_0003

0001	set -x; $TESTBIN/flash_erase $MTD_CHAR_DEV1 0 1 && exit 0 || exit 1
0002	set -x; $TESTBIN/flash_erase $MTD_CHAR_DEV1 0 4 && exit 0 || exit 1
0003	set -x; $TESTBIN/flash_eraseall $JFFS_OPTIONS $MTD_CHAR_DEV1 && exit 0 || exit 1
0004 	set -x; $TESTBIN/flash_erase $MTD_CHAR_DEV1 $(echo $DEV_SIZE-4*$BLOCK_SIZE|bc) 5 && exit 1 || exit 0

# End L_DD_NAND_0003
