###################################################################################################################
# Scenario: L_DD_MMC_0007A
# Author  : Misael Lopez Cruz
# Date    : September 20, 2006
# Description: Test if MMC device is recongnized when it is present on the slot
###################################################################################################################

# Begin L_DD_MMC_0007A

0001_a 	cat /proc/devices | grep 'mmc' && exit 0 || exit 1
0001_b	cat /proc/interrupts | grep 'mmc' && exit 0 || exit 1
0001_c	ls /dev/mmc* && exit 0 || exit 1
0001_d	cat /proc/partitions | grep 'mmc' && exit 0 || exit 1

# End L_DD_MMC_0007A
