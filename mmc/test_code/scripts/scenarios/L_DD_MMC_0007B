###################################################################################################################
# Scenario: L_DD_MMC_0007B
# Author  : Misael Lopez Cruz
# Date    : September 20, 2006
# Description: Test that driver doesn't recognize any MMC device when they are not present
###################################################################################################################

# Begin L_DD_MMC_0007B

0002_a 	cat /proc/devices | grep 'mmc' && exit 0 || exit 1
0002_b	cat /proc/interrupts | grep 'mmc' && exit 0 || exit 1
0002_c	ls /dev/mmc && exit 1 || exit 0
0002_d	cat /proc/partitions | grep 'mmc' && exit 1 || exit 0

# End L_DD_MMC_0007B

