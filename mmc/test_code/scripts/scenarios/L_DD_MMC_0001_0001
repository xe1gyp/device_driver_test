################################################################################
# Scenario: L_DD_MMC_0001_0001
# Author  : Misael Lopez Cruz
# Date    : September 20, 2006
# Description: Test if MMC device is recongnized when it is present on the slot
################################################################################

# Begin L_DD_MMC_0001_0001

00 set -x; cat /proc/devices | grep 'mmc' && exit 0 || exit 1
02 set -x; cat /proc/interrupts | grep 'mmc' && exit 0 || exit 1
03 set -x; ls /dev/mmc* && exit 0 || exit 1
04 set -x; cat /proc/partitions | grep 'mmc' && exit 0 || exit 1

# End L_DD_MMC_0001_0001
