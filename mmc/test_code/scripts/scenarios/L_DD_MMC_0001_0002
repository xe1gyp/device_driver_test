###################################################################################################################
# Scenario: L_DD_MMC_0001_0002
# Author  : Misael Lopez Cruz
# Date    : September 20, 2006
# Description: Test that driver doesn't recognize any MMC device when they are not present
###################################################################################################################

# Begin L_DD_MMC_0001_0002

01 set -x; ls /dev/mmc && exit 1 || exit 0
02 set -x; cat /proc/partitions | grep 'mmc' && exit 1 || exit 0

# End L_DD_MMC_0001_0002
