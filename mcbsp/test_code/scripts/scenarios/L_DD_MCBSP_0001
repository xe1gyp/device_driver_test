##############################
# File Name   : L_DD_MCBSP_0001
# Author      : Abraham Arce
# Date        : March 08, 2007
# Description : McBSP Test Suite
###############################

# Begin L_DD_MCBSP_0001

#This test case verifies if the McBSP driver supports full duplex transmission of data using DMA 
0001 set -x;testmcbsp.sh "0001_0001" "McBSP ID" "5" "" "None" && exit 0 || exit 1

#The McBSP driver supports configuring the sampling rate, this test case verifies the same.
0002 set -x; testmcbsp.sh "0001_0002" "Sampling Rate (frequency, hertz)" "7" "sample_rate=" "$SAMPLE_RATES" && exit 0 || exit 1

# End L_DD_MCBSP_0001
