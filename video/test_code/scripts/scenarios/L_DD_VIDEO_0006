#####################
# FILENAME: L_DD_VIDEO_0006
# Author  : Benjamin Mejia
# Date    : Fri Jan 5, 2007

# Begin "L_DD_VIDEO_0006"


INFO comment.sh "If some of these tests fail, reboot your board"

0002_VD1 set -x; transparency.sh 1 "RGB565 160 120" "1 0 63489" $VIDEOFILES/video_qqvga_rgb_30  && exit 0 || exit 1
0002_VD1 set -x; transparency.sh 2 "RGB565 160 120" "1 0 63489" $VIDEOFILES/video_qqvga_rgb_30  && exit 0 || exit 1

INFO comment.sh "Reboot is needed to clean the display"

# End "L_DD_VIDEO_0006"
