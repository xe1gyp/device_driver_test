#####################
# FILENAME: L_DD_VIDEO_0010
# Author  : Cristopherson Torres Martinez
# Date    : Fri Feb 6, 2009

# Begin "L_DD_VIDEO_0010"

# DSS pipelines shall be capable of being assigned dynamically to either LCD or TV-out, this test verifies the same in video pipeline 1.
0001_COMMENT comment.sh "Verify Video Pipeline 1 can be displayed on TV and LCD alternately"
0001 set -x; dss_streaming.sh 1 1 && exit 0 || exit 1

# DSS pipelines shall be capable of being assigned dynamically to either LCD or TV-out, this test verifies the same in video pipeline 2.
0002_COMMENT comment.sh "Verify Video Pipeline 2 can be displayed on TV and LCD alternately"
0002 set -x; dss_streaming.sh 2 1 && exit 0 || exit 1

# End "L_DD_VIDEO_0010"
