################################################################################
# Scenario: L_DD_KEYPAD_0001_0001
# Author  : Abraham Arce
# Date    : Sat May 17 17:47:01 CDT 2008
# Description: 
################################################################################

# Begin L_DD_KEYPAD_0001_0001

0001 echo "This test validates whether the keypad driver is sending events to the Linux Input Sub System"; GeneralTestingKeyPad.sh "showkey"  && exit 0 || exit 1
0002 echo "This test validates the keypad driver's autorepeat feature"; GeneralTestingKeyPad.sh "testkeypad" && exit 0 || exit 1
0003 echo "This test validates the keypad driver's multiple key support"; GeneralTestingKeyPad.sh "testkeypad" && exit 0 || exit 1

# End L_DD_KEYPAD_0001_0001
