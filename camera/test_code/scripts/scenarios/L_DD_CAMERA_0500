#####################
# FILENAME: L_DD_CAMERA_0500
# Author  : Ivaylo Petrov
# Date    : Tue Jun 23, 2010
# Copyright (C) 2010 Texas Instruments
#
# Description:
# Camera (CAMERA) Previewer wrapper driver for HQ capture

# Begin "L_DD_CAMERA_0500"

COMMENT comment.sh "Test omap-previewer with Red/Green/Blue WBAL coefficients"

0001_SAN set -x; prvhq.sh "8MP" "omap-previewer" "WBAL" "red" && exit 0 || exit 1
0002_SAN set -x; prvhq.sh "8MP" "omap-previewer" "WBAL" "green" && exit 0 || exit 1
0003_SAN set -x; prvhq.sh "8MP" "omap-previewer" "WBAL" "blue" && exit 0 || exit 1
0004 set -x; prvhq.sh "5MP" "omap-previewer" "WBAL" "red" && exit 0 || exit 1
0005 set -x; prvhq.sh "5MP" "omap-previewer" "WBAL" "green" && exit 0 || exit 1
0006 set -x; prvhq.sh "5MP" "omap-previewer" "WBAL" "blue" && exit 0 || exit 1
0007 set -x; prvhq.sh "XGA" "omap-previewer" "WBAL" "red" && exit 0 || exit 1
0008 set -x; prvhq.sh "XGA" "omap-previewer" "WBAL" "green" && exit 0 || exit 1
0009 set -x; prvhq.sh "XGA" "omap-previewer" "WBAL" "blue" && exit 0 || exit 1
0010_SAN set -x; prvhq.sh "VGA" "omap-previewer" "WBAL" "red" && exit 0 || exit 1
0011_SAN set -x; prvhq.sh "VGA" "omap-previewer" "WBAL" "green" && exit 0 || exit 1
0012_SAN set -x; prvhq.sh "VGA" "omap-previewer" "WBAL" "blue" && exit 0 || exit 1

# End "L_DD_CAMERA_0500"

