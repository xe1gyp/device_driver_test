#####################
# FILENAME: L_DD_CAMERA_0216
# Author  : Ivaylo Petrov
# Date    : Tue Jun 28, 2010
# Copyright (C) 2010 Texas Instruments
#
# Description:
# Camera (CAMERA) Test Resizer driver for HQ capture

# Begin "L_DD_CAMERA_0216"

COMMENT comment.sh "Test if QVGA upscale is supported up to x4."

#0001_SAN set -x; rszhq.sh "QVGAto8MP" && exit 0 || exit 1
#0002 set -x; rszhq.sh "QVGAto5MP" && exit 0 || exit 1
#0003 set -x; rszhq.sh "QVGAtoQXGA" && exit 0 || exit 1
#0004 set -x; rszhq.sh "QVGAtoUXGA" && exit 0 || exit 1
0005_SAN set -x; rszhq.sh "QVGAtoSXGA" && exit 0 || exit 1
0006 set -x; rszhq.sh "QVGAtoXGA" && exit 0 || exit 1
0007 set -x; rszhq.sh "QVGAtoSVGA" && exit 0 || exit 1
0008 set -x; rszhq.sh "QVGAtoVGA" && exit 0 || exit 1

# End "L_DD_CAMERA_0216"

