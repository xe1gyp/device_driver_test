#####################
# FILENAME: L_DD_CAMERA_0215
# Author  : Ivaylo Petrov
# Date    : Tue Jun 28, 2010
# Copyright (C) 2010 Texas Instruments
#
# Description:
# Camera (CAMERA) Test Resizer driver for HQ capture

# Begin "L_DD_CAMERA_0215"

COMMENT comment.sh "Test if QCIF upscale is supported up to x4."

#0001_SAN set -x; rszhq.sh "QCIFto8MP" && exit 0 || exit 1
#0002 set -x; rszhq.sh "QCIFto5MP" && exit 0 || exit 1
#0003 set -x; rszhq.sh "QCIFtoQXGA" && exit 0 || exit 1
#0004 set -x; rszhq.sh "QCIFtoUXGA" && exit 0 || exit 1
#0005_SAN set -x; rszhq.sh "QCIFtoSXGA" && exit 0 || exit 1
#0006 set -x; rszhq.sh "QCIFtoXGA" && exit 0 || exit 1
#0007 set -x; rszhq.sh "QCIFtoSVGA" && exit 0 || exit 1
0008 set -x; rszhq.sh "QCIFtoVGA" && exit 0 || exit 1
0009_SAN set -x; rszhq.sh "QCIFtoQVGA" && exit 0 || exit 1

# End "L_DD_CAMERA_0215"

