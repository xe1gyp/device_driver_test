#####################
# FILENAME: L_DD_CAMERA_0023
# Author  : Omar Esli Jimenez Villarreal
# Date    : Tuesday August 08, 2008
# Copyright (C) 2008 Texas Instruments
#
# Description:
# Camera (CAMERA) Test multiple file handles to be open simultaneously to the camera device.

# Begin "L_DD_CAMERA_0023"

COMMENT comment.sh "Video captured should be displayed cleanly on LCD, and the brigthness will change while capturing the video"

0001 set -x; multiopen.sh "1"&& exit 0 || exit 1
0002 set -x; multiopen.sh "2"&& exit 0 || exit 1
0003 set -x; multiopen.sh "3"&& exit 0 || exit 1
0004 set -x; multiopen.sh "4"&& exit 0 || exit 1
0005 set -x; multiopen.sh "5"&& exit 0 || exit 1
0006 set -x; multiopen.sh "6"&& exit 0 || exit 1

# End "L_DD_CAMERA_0023"
