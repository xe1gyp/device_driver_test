#####################
# FILENAME: L_DD_CAMERA_0217
# Author  : Ivaylo Petrov
# Date    : Tue Jun 28, 2010
# Copyright (C) 2010 Texas Instruments
#
# Description:
# Camera (CAMERA) Test Resizer driver for HQ capture

# Begin "L_DD_CAMERA_0217"

COMMENT comment.sh "Test if VGA upscale is supported up to x4."

#0001_SAN set -x; rszhq.sh "VGAto8MP" && exit 0 || exit 1
#0002 set -x; rszhq.sh "VGAto5MP" && exit 0 || exit 1
0003 set -x; rszhq.sh "VGAtoQXGA" && exit 0 || exit 1
0004 set -x; rszhq.sh "VGAtoUXGA" && exit 0 || exit 1
0005_SAN set -x; rszhq.sh "VGAtoSXGA" && exit 0 || exit 1
0006 set -x; rszhq.sh "VGAtoXGA" && exit 0 || exit 1
0007 set -x; rszhq.sh "VGAtoSVGA" && exit 0 || exit 1

# End "L_DD_CAMERA_0217"

