###############################################################################
# Scenario    : L_DD_DEMO_0004
# Author      : Name of Author
# Date        : Thu Sep 17 11:25:35 CDT 2009
# Description : Process Priority
###############################################################################

# Begin L_DD_DEMO_0004

01  set -x; $UTILS_DIR_HANDLERS/handlerProcessPriority.sh "add" "1" "dd if=/dev/urandom of=/tmp/temp1 bs=1K count=100" "+15"
02  set -x; $UTILS_DIR_HANDLERS/handlerProcessPriority.sh "add" "2" "dd if=/dev/urandom of=/tmp/temp2 bs=1K count=100" "-15"
03  set -x; $UTILS_DIR_HANDLERS/handlerProcessPriority.sh "execute" "20"
04  set -x; $UTILS_DIR_HANDLERS/handlerProcessPriority.sh "verify"

# End L_DD_DEMO_0004