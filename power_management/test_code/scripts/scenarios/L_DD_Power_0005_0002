###################################################################################################################
# Scenario: L_DD_Power_0005_0002
# Author  : Ricardo Perez Olivares
# Date    : 02 Sep 08
# Description: Test for Shared Resource Framework 
###################################################################################################################



#Begin L_DD_Power_0005_0002

0002_a  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x1 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_b  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x2 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_c  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x3 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_d  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x4 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_e  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x5 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_f  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x6 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_g  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x7 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

0002_h  insmod $TESTMODS/resource_test.ko opt1=0x2 opt2=0x8 && rmmod resource_test.ko && echo "VALIDATION CRITERIA: Verify that \"Test Passed\" message is printed" && sleep 1 && $UTILBIN/akey && sleep 2 && echo "Reboot the system after this test" && sleep 1 && $UTILBIN/waitkey && exit 0 || exit 1

#End L_DD_Power_0005_0002
